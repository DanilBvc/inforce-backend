export const baseUrl = 'https://dull-rose-goat-cape.cyclic.app';

export const uploadImg = baseUrl + '/upload';
export const products = baseUrl + '/products';
export const productById = (id: string) => baseUrl + `/product/${id}`;
